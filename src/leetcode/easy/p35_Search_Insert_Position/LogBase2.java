package leetcode.easy.p35_Search_Insert_Position;

public class LogBase2 {
    public static double Calculate(double x) {
        return Math.log(x) / Math.log(2);
//        System.out.println("log2(" + x + ") = " + logBase2);
    }
}
